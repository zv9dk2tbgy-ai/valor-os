[build]
  command = "npm run build"
  publish = "dist"

# The app keeps all navigation in client-side state (no client-side router),
# so a single-page fallback isn't required, but this makes deep links safe.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
